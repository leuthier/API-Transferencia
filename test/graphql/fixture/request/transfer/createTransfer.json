{
    "query": "mutation Transfer($fromEmail: String!, $toEmail: String!, $amount: Float!) { transfer(fromEmail: $fromEmail, toEmail: $toEmail, amount: $amount) { id from { id email } to { id email } amount createdAt } }",
    "variables": {
        "fromEmail": "string",
        "toEmail": "victor@leuth.com",
        "amount": 10
    }
}