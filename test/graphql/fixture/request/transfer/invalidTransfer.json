{
    "query": "mutation Transfer($fromEmail: String!, $toEmail: String!, $amount: Float!) { transfer(fromEmail: $fromEmail, toEmail: $toEmail, amount: $amount) { id } }",
    "variables": {
        "fromEmail": "string2",
        "toEmail": "victor@leuth.com",
        "amount": 10
    }
}